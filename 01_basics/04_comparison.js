// console.log("2" > 1);
// console.log("02" > 1);


// console.log(null > 0); // false
// console.log(null == 0); // false
// console.log(null >= 0); // true

// The reason is that an equality check (==) and comparison (>, <, >=, <=) work differently in JavaScript,
// Comparison operators (like >, <, >=, <=) convert null to a number (0) for the comparison.

console.log("2" === 2);
